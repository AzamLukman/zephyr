menuconfig LORAGW
	bool "LORAGW"
	depends on SPI
	help
	  enable driver for LORAGW.
if LORAGW

module = LORAGW
module-str = loragw
source "subsys/logging/Kconfig.template.log_config"

config LORAGW_INIT_PRIORITY
	int "LoRa initialization priority"
	default 90
	help
	  System initialization priority for LoRa drivers.

endif
